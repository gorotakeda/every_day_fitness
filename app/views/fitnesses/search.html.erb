<h1>
  検索結果
</h1>
<%# 検索結果の個数で条件分岐 %>
<% if @results.length !=0 %>
  <% @results.each do |fitness| %>
  <li class='item-lists'>
      <%= link_to fitness_path(fitness.id), method: :get do %>
        <div class='item-info'>
          <h3 class='item-name'>
            <%= fitness.title %>
          </h3>
          <h3 class='item-name'>
            <%= fitness.user.nickname %>
          </h3>
          <div class='item-price'>
            <span><%= fitness.category.name %><br><%= fitness.level.name %></span>
          </div>
        </div>
        <% end %>
  <% end %>
<% else %>
  該当するコンテンツはありません
<% end %>
<%= link_to 'トップページへ戻る', root_path %>